Flask==3.1.3
Flask-SQLAlchemy==3.1.1
alembic==1.18.5
# Pinned explicitly (alembic's own transitive dependency): Mako 1.4.0
# accidentally ships its internal nox test-tooling as a top-level installable
# `tools` package, which shadows this repo's own `tools/` package and breaks
# every test that imports from `tools.*`. Not a security issue -- verified
# the shipped files are legitimate Mako-repo nox helpers -- just a packaging
# mistake upstream. Pinned to the last release before it.
Mako==1.3.12
Flask-Login==0.6.3
Authlib==1.7.2
ldap3==2.9.1
requests==2.34.2
psycopg[binary]==3.3.4
gunicorn==26.0.0
Werkzeug==3.1.8
setuptools==83.0.0
msgpack==1.2.1
argon2-cffi==25.1.0
pyotp==2.10.0
pysnmp==7.1.28
dnspython==2.8.0
boto3==1.40.4
webauthn==2.7.1
httpx[http2]==0.28.1
