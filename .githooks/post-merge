#!/usr/bin/env bash
set -Eeuo pipefail

# Pulling source must never mutate a running environment. ServiceOps is
# delivered to the governed MicroK8s target only after release verification.
printf '%s\n' 'Source updated. Run QA/release, then the verified MicroK8s deployment gate.'
